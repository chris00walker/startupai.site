{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "version": "1.0",
  "project_name": "StartupAI Marketing Site",
  "description": "Static Next.js 15 marketing site - SEO-optimized lead capture",

  "security": {
    "mode": "strict",
    "deny_file_access": [
      ".env*",
      "*.pem",
      "*.key",
      "netlify.toml",
      ".netlify/**",
      "node_modules/**",
      ".git/**"
    ],
    "deny_file_edit": [".env*", "netlify.toml"],
    "sensitive_patterns": [
      "SUPABASE_ANON_KEY",
      "POSTHOG_KEY",
      "RESEND_API_KEY",
      "JWT_SECRET"
    ]
  },

  "permissions": {
    "allowed_commands_no_confirm": [
      "git status",
      "git log",
      "git diff",
      "pnpm dev",
      "pnpm lint",
      "pnpm type-check",
      "pnpm format:check",
      "ls",
      "cat",
      "pwd"
    ],
    "require_confirmation": [
      "pnpm add*",
      "pnpm remove*",
      "pnpm install",
      "pnpm build",
      "git push*",
      "git commit*",
      "netlify deploy*",
      "rm *",
      "mv *"
    ],
    "blocked_commands": ["sudo*", "chmod*", "npm*"]
  },

  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "pnpm type-check --noEmit"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "pnpm type-check --noEmit"
          }
        ]
      }
    ],
    "Notification": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Next.js 15: Ensure page.tsx uses correct client/server pattern'"
          }
        ]
      }
    ]
  },

  "project_context": {
    "framework": "Next.js 15 (App Router)",
    "architecture": "Static Export (JAMstack)",
    "deployment": "Netlify",
    "node_version": "18+",
    "package_manager": "pnpm",
    "purpose": "Marketing and lead capture",
    "constraints": [
      "NO server-side code (static export only)",
      "NO API routes (use Netlify Functions)",
      "NO dynamic rendering",
      "Formspree for form submissions"
    ]
  },

  "coding_standards": {
    "component_structure": "Shadcn New York theme",
    "css_framework": "Tailwind CSS (shared design tokens)",
    "form_validation": "React Hook Form + Zod",
    "analytics": "PostHog (client-side only)",
    "seo": "Next.js Metadata API"
  },

  "workflow": {
    "planning_mode": "hybrid",
    "require_plan_for": [
      "new_pages",
      "routing_changes",
      "form_submissions",
      "analytics_events"
    ],
    "yolo_mode_for": [
      "typo_fixes",
      "style_tweaks",
      "content_updates",
      "comment_additions"
    ]
  }
}
